# Open Decisions

Non-clinical, non-code items that need a human decision before pilot/launch
(playbook Section 9). Engineering ambiguities are instead resolved via ADRs
in `docs/adr/` per rule R9 and don't appear here.

| # | Item | Owner needed | Status |
|---|------|--------------|--------|
| 1 | Consent + legal texts, all 5 languages (EN/DE/TR/RU/AR) | Legal | Not started |
| 2 | DE translations review (agent drafts are `MACHINE_DRAFT`) | Clinical/native reviewer | Not started |
| 3 | Out-of-hours red-flag protocol copy | Clinical | Not started |
| 4 | Drug database licensing (seed uses a public generic-name list only, `PLACEHOLDER`) | Product/Legal | Not started |
| 5 | Real SMS/email provider contracts (EU region) | Ops/Legal | Not started |
| 6 | LLM provider DPA/AVV execution + final model choice (from `AI_EVAL_REPORT.md`) | Legal/Eng lead | `AI_EVAL_REPORT.md` exists but is a local stub-provider run (ADR-0007) — 100% on all four thresholds, but that only validates the guardrail/pipeline wiring, not real model behavior. Real Bedrock/Azure creds needed for a credentialed CI-nightly run before this can inform a final model choice. |
| 7 | DPIA (Data Protection Impact Assessment) | Legal/DPO | Not started |
| 8 | Data retention policy confirmation | Legal/DPO | Not started |
| 9 | Companion SRS document not present in repo — several playbook sections cite it verbatim (ruleset thresholds, FR/AI/NFR text) | Product | Locate and attach `NachCareAI_MVP_Specification.md` |
| 10 | Rails 7.2 (locked stack decision, Section 2) reaches end-of-life 2026-08-09 — one week from today. Brakeman flags this; suppressed via `backend/config/brakeman.ignore` with a note so CI stays actionable instead of permanently red. | Eng lead | Plan the Rails 7.3/8.x upgrade; don't let this ignore rule go stale |
| 11 | Rack::Attack (M7 hardening, ADR-0009 #6) uses the gem's default in-process `ActiveSupport::Cache::MemoryStore`, not the app's Redis instance — deliberate, to avoid coupling rate-limit *availability* to Redis health, but it means limits don't coordinate across horizontally-scaled backend processes. This build runs a single backend container, so the gap is dormant today. | Eng lead | Move Rack::Attack to a shared (Redis-backed, fail-open-on-error) store before running more than one backend process/dyno in production. |
| 12 | M7's load gate (`ops/k6/load_checkins.js`, ADR-0009 #9) — "300 concurrent check-ins < 2s p95" — does not pass in this local Docker Desktop dev environment even after Puma tuning (`WEB_CONCURRENCY=10`/`RAILS_MAX_THREADS=6`, swept several configs): converges around p95 ~2.3-2.4s. Backend container CPU never exceeded ~50% during any run, ruling out raw compute; most likely Docker Desktop's virtualized network/CPU scheduling overhead for a single shared container, not an application defect (individual request latency under light load is 69-89ms). | Eng lead | Re-run the load gate against a real deployment target (dedicated host or multiple containers behind a load balancer) before treating "<2s p95 @ 300 concurrent" as met — this session's environment cannot prove or disprove it definitively either way. |
| 13 | Post-M7 (ADR-0010 #8a): `WEB_CONCURRENCY=10` was removed from `ops/docker-compose.yml`'s persistent `backend` service — it was silently breaking every ActionCable feature (`config/cable.yml`'s `async` adapter only delivers a broadcast within the process that called it, so 10 Puma workers meant a live update almost never reached the worker holding a given client's connection), discovered while manually verifying this phase's realtime caregiver-activity feed. The standing dev/demo backend is single-process again (ActionCable works correctly; this is now the manually-verified state). | Eng lead | Re-supply `WEB_CONCURRENCY=10` explicitly (e.g. `docker compose run -e WEB_CONCURRENCY=10 ...` or a compose override) whenever re-running `ops/verify_m7.sh`'s k6 load gate — it no longer gets the M7 tuning by default. |
| 14 | Promotion of the shadow risk model (`Domain::Risk::Scorer`, UC-05/21/23) to real production/clinical use — i.e. a promotion decision backed by real gate-passing pilot data, not the dev/demo override (`docs/adr/0012-shadow-risk-scoring-and-promotion.md`) — is a genuine human/clinical decision gate, separate from and not satisfied by the in-product dev/demo override that exists so this environment can demonstrate the UC-23-25 experience. No real pilot/outcomes data exists in this build to ever pass the gates for real. | Clinical/Product | Not started — requires a real multi-week pilot generating real (rules-verdict, risk-score, outcome) triples before `Domain::Risk::PromotionGate` could honestly report `overall_met: true` anywhere. |
| 15 | Real (non-EU, dev/test-only) LLM providers now wired for this dev environment — direct Anthropic API (no usable account credit as of writing) and direct Google Gemini API (active, working, free-tier-rate-limited) — see ADR-0014 and `docs/SUBPROCESSORS.md`'s dev-only exceptions section. Item #6 above (production provider DPA/AVV + final model choice) is the real gate for production; this row is just a heads-up that #6's "no live credentials yet" framing is now stale for *dev*, not for production. | Legal/Eng lead | Not started for production — #6 above still governs. If Anthropic's account gets credited, or if either provider is ever considered for real use, revisit R8's EU-only requirement (neither has an EU region today). |
