# Open Clinical Items

Live log of every place the build substitutes a `PLACEHOLDER_CLINICAL` value
because the source-of-truth SRS content wasn't available at build time, per
rule R1 (never invent clinical content) and R9 (surface, don't guess, on
anything clinical/regulatory). Every row here must become a config/content
swap before clinical launch — never a code change.

| # | Item | Where | Placeholder used | Needs |
|---|------|-------|-------------------|-------|
| 1 | Companion SRS (`NachCareAI_MVP_Specification.md`) is not present in the repo. All "verbatim from SRS" content below (ruleset thresholds, FR/AI/NFR requirement text, alert/emergency copy, consent texts) is therefore drafted by the agent as `PLACEHOLDER_CLINICAL`, not sourced from the SRS or from training knowledge. | Whole build | N/A | Locate/attach the SRS; re-derive every placeholder row against it before clinical sign-off. |
| 2 | Local drug table (`backend/db/seeds/drugs.rb`, 202 rows) is a hand-compiled list of public generic (INN) medication names by pharmacological class, seeded per the playbook's own instruction ("200 common German cardiac meds you seed from a public generic list — names only, PLACEHOLDER for licensed DB"). No dosing, interaction, or licensing data. | M1 enrollment med type-ahead | `Drug` model + seed rows, all treated as placeholder | Licensed drug DB (see `docs/OPEN_DECISIONS.md` #4) before clinical launch — swap the seed source, not the type-ahead code. |
| 3 | Escalation ruleset R-1…R-11: every threshold (weight-gain kg, day windows, consecutive-day counts), every action/alert copy string except the 3 the playbook gives verbatim (`low-salt day`, `track fluids 1.5 L`, `elevate legs + photo`), and all 30 `red_flag_phrases` (EN+DE) are deliberately-absurd sentinels, not clinical values. Rule *topics* for R-4 (breathless-at-rest), R-8 (missed check-in), R-10 (red-flag phrases) are confirmed by the playbook text; all other rule topics/thresholds are an agent structural decision (ADR-0005), not from SRS Section 6.2. | M2 escalation engine | `backend/config/rulesets/ruleset_v0_1.json` (`placeholder_clinical: true` on every rule) | Full SRS Section 6.2 content + clinical sign-off before any real patient use — this is the single highest-priority item before clinical launch. |
| 4 | Check-in symptom question wording (beyond the `breathless_at_rest` key name, confirmed by the playbook) is not sourced from SRS/approved UI screens. | M2 caregiver check-in UI | i18n keys scaffolded, EN copy marked placeholder in-file | Real symptom question wording from SRS/approved designs. |
| 5 | Nurse→caregiver message template bank: reuses the same 3 playbook-verbatim strings as the ruleset (`low-salt day`, `track fluids 1.5 L`, `elevate legs + photo`) plus one clinical-instruction template (`call_nurse_now`) that has no approved copy anywhere, using the same `[PLACEHOLDER_CLINICAL: ...]` marker already established in the ruleset's `action_templates`. The other two bank entries (`checkin_reminder`, `thank_you`) are ordinary operational app copy, not clinical content. | M4 nurse messages | `backend/app/domain/messages/templates.rb` (`BANK["call_nurse_now"]`) | Real approved call-nurse-now copy before clinical launch. |
| 6 | ~~M4's "translate-assist show-before-send" flow ships a non-AI stub~~ — **resolved in M5**: `Domain::Messages::TranslateAssist` now calls the real T-TRANSLATE pipeline (`Domain::Ai::Tasks::Translate`); nurse-reviews-before-send contract unchanged, `nil` on degradation preserved. Kept here (not deleted) as the historical record of the M4->M5 transition. | M4->M5 nurse messages | `backend/app/domain/messages/translate_assist.rb` | None — resolved. |
| 7 | AI gateway prompt templates (`config/prompts/*.md`): the assistant system prompt's scope/refusal/style rules are structural (SRS AI-10 persona rules per the playbook), but any implied clinical framing (what counts as "in-scope guidance," example phrasing for refusals) is not sourced from the SRS and is marked `<!-- PLACEHOLDER_CLINICAL -->` in each file. | M5 AI gateway | `backend/config/prompts/{assistant_system,brief,triage,callnote,report}.md` | Medical sign-off on persona/scope/style rules and any example phrasing before clinical launch. |
| 8 | `EmergencyDetector`'s 5-language phrase list is a second set of deliberately-absurd `PLACEHOLDER_CLINICAL` sentinels (`config/ai_emergency_phrases.yml`), separate from the R-10 `red_flag_phrases` in the escalation ruleset (#3 above) because the assistant needs full 5-language coverage while R-10's list only covers EN+DE with an EN-fallback contract that a shared file would have broken (see ADR-0007). Same category of placeholder content as #3, tracked here as one entry rather than duplicated. | M5 assistant `EmergencyDetector` | `backend/config/ai_emergency_phrases.yml` | Same as #3 — real clinically-reviewed emergency phrasing per language before clinical launch; this file and the ruleset's `red_flag_phrases` should likely converge on one clinically-authored source once real content exists. |
| 9 | Seeded demo knowledge base (`db/seeds/knowledge_base.rb`, originally 2 docs: "Fluid Tracking Guide", "Low-Salt Day Guide") is placeholder app-guidance content the agent wrote so the assistant's RAG retrieval has something real to cite in a fresh dev/demo checkout — not sourced from the SRS or any approved patient-education material. | M5 knowledge base | `backend/db/seeds/knowledge_base.rb` (bodies marked `[PLACEHOLDER_CLINICAL]` inline) | Replace with real, clinically-approved patient-education content authored/reviewed through the FR-N15 two-person approval CMS flow before clinical launch — the seed seam is exactly the `KnowledgeDoc` rows, no code change needed. |
| 12 | Post-M7 product-owner feedback item #4: expanded the seeded knowledge base with 5 more placeholder docs ("Eating Out and Travel Guide", "Activity and Exercise Guide", "Bathing and Hygiene Guide", "Sleep and Rest Guide", "Visitors and Social Activity Guide") covering everyday post-discharge caregiver questions the original 2-doc KB had zero coverage for (retrieval returned no matches for realistic questions like "Can she eat normal restaurant food this weekend?", so the assistant always fell through to the routed/out-of-scope reply even though the question was genuinely in-scope). Same `[PLACEHOLDER_CLINICAL]` convention, same generic/conservative/non-prescriptive tone as the original 2 docs, authored without a real SRS or clinical sign-off. Also motivated a `StubProvider` retrieval-scoring tuning (added "she"/"her"/"he"/"him" to its stopword list — an engineering artifact, not clinical content, see `docs/adr/0013-*.md`). | Post-M7 assistant feedback | `backend/db/seeds/knowledge_base.rb` (5 new docs, bodies marked `[PLACEHOLDER_CLINICAL]` inline) | Same as row 9 — replace with real, clinically-approved patient-education content authored/reviewed through the FR-N15 two-person approval CMS flow before clinical launch. |
| 13 | New `Domain::Ai::Tasks::ExplainCarePlanItem` task (post-M7 product-owner feedback item #1) explains an already-prescribed care-plan item (medication schedule/instructions, or the nurse's free-text `care_instructions`/`diet_rules`) to the caregiver in plain language. Its prompt template (`config/prompts/explain_care_plan_item.md`) is structural (constrains the model to explain-only, never advise a change — R3), but any implied clinical framing in the template's own wording is not sourced from the SRS, same category as row 7. | Post-M7 caregiver-home feedback | `backend/config/prompts/explain_care_plan_item.md` | Medical sign-off on persona/scope/style rules before clinical launch, same as row 7. |
| 10 | Seeded Learn curriculum (`db/seeds/content_items.rb`, 4 items across weeks 1/2/4/8: "Getting started", "Reading your weight trend", "Staying on track with medications", "Looking ahead to day 90") is placeholder patient-education content the agent wrote so the Learn screen has real, unlockable content in a fresh dev/demo checkout — not sourced from the SRS or any approved curriculum. Same category and same seam as #9 (the two-person `ContentItem#approve!` CMS flow already exists for the real swap). | M6 Learn curriculum | `backend/db/seeds/content_items.rb` (bodies marked `[PLACEHOLDER_CLINICAL]` inline), `backend/app/models/content_item.rb` | Replace with real, clinically-approved curriculum content (title + body per language, per `week_no`) authored/reviewed through the same CMS approval flow before clinical launch — no code change needed, only new `ContentItem` rows. |

| 11 | `Domain::Risk::Scorer`'s heuristic trajectory-scoring formula (UC-05/UC-23): weight-velocity/symptom-drift/adherence-gap component weights (0.5/0.3/0.2), the 3-day/1.2kg velocity window+cap, the 3-check-in symptom-drift window, the 7-day adherence window, the 0.55 `ALERT_GATE` and 0.35 `ELEVATED_THRESHOLD` thresholds — all placeholder MVP values standing in for a real trained model, exactly parallel to the ruleset's `999`-kg sentinels (#3 above). **Not a validated clinical predictor** — never state or imply otherwise in code, UI copy, or docs. No pilot/labeled-outcomes data exists to train or validate a real model against. | M-post-M7 (UC-05/21/23/24/25) shadow risk scoring | `backend/app/domain/risk/scorer.rb` (constants + inline rationale) | Real trained-model replacement (or clinically-reviewed heuristic recalibration) once real pilot outcome data exists — see `docs/adr/0012-shadow-risk-scoring-and-promotion.md` and `docs/OPEN_DECISIONS.md` (promotion-to-production gate). |

_(Rows are appended here as each phase introduces clinical content — consent/legal texts already covered above.)_
