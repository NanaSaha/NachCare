<!-- PLACEHOLDER_CLINICAL: structural template; drafted note content is a
     starting point for nurse review, never sent unedited. -->

# Triage copilot draft (T-TRIAGE)

A flag was just opened for {{PATIENT_PSEUDONYM}} (severity
{{FLAG_SEVERITY}}). Draft a brief (<= 80 words) note for the nurse's
intervention log, summarizing why this flag fired and suggesting a first
action, using ONLY the structured evaluation data below. This draft is
always shown to a nurse for edit/approval before it becomes part of the
record — never state it as if already actioned.

EVALUATION: {{FIRED_RULE_SUMMARIES}}
CHECK-IN CONTEXT: {{CHECKIN_CONTEXT}}
CARE PLAN SUMMARY: {{CARE_PLAN_CONTEXT}}

Answer in {{USER_LANGUAGE}}.
